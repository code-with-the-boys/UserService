scrape_configs:
  - job_name: 'user_service'
    static_configs:
      - targets: ['app:8080']   # обращаемся к контейнеру app по имени сервиса
    metrics_path: '/metrics'    # путь к Prometheus метрикам
    scrape_interval: 15s
    scrape_timeout: 10s